<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd" >
<mapper namespace="com.kwr.spring.facetestbackend2.mappers.CommentMapper" >
    <insert id="insert">
        INSERT INTO facevisitor.comments (page_type, nickname, password_hash, content)
        VALUES (#{pageType}, #{nickname}, #{passwordHash}, #{content})
    </insert>
    <update id="updateContent">
        UPDATE facevisitor.comments
        SET content = #{content}
        WHERE id = #{id}
          AND password_hash = #{passwordHash}
          AND deleted = 0
    </update>
    <update id="softDelete">
        UPDATE facevisitor.comments
        SET deleted = 1
        WHERE id = #{id}
          AND password_hash = #{passwordHash}
    </update>
    <select id="getByPageType" resultType="com.kwr.spring.facetestbackend2.entities.CommentEntity">
        SELECT *
        FROM facevisitor.comments
        WHERE page_type = #{pageType}
          AND deleted = 0
        ORDER BY created_at DESC
    </select>
</mapper>