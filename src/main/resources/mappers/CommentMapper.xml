<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper
        PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.kwr.spring.facetestbackend2.mappers.CommentMapper">

    <!-- 댓글 추가 -->
    <insert id="insert" parameterType="com.kwr.spring.facetestbackend2.entities.CommentEntity" useGeneratedKeys="true" keyProperty="id">
        INSERT INTO comments (post_id, nickname, password, content, created_at, updated_at)
        VALUES (#{postId}, #{nickname}, #{password}, #{content}, NOW(), NOW())
    </insert>

    <!-- 댓글 수정 -->
    <update id="update" parameterType="com.kwr.spring.facetestbackend2.entities.CommentEntity">
        UPDATE comments
        SET content = #{content}, updated_at = NOW()
        WHERE id = #{id}
    </update>

    <!-- 댓글 삭제 -->
    <delete id="delete" parameterType="long">
        DELETE FROM comments
        WHERE id = #{id}
    </delete>

    <!-- 특정 게시물의 댓글 목록 조회 -->
    <select id="selectByPostId" parameterType="long" resultType="com.kwr.spring.facetestbackend2.entities.CommentEntity">
        SELECT id, post_id, nickname, password, content, created_at, updated_at
        FROM comments
        WHERE post_id = #{postId}
        ORDER BY created_at DESC
    </select>

    <!-- 단일 댓글 조회 -->
    <select id="selectById" parameterType="long" resultType="com.kwr.spring.facetestbackend2.entities.CommentEntity">
        SELECT id, post_id, nickname, password, content, created_at, updated_at
        FROM comments
        WHERE id = #{id}
    </select>

</mapper>
